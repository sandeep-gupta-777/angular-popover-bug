import { SortBotsPipe } from './sort-bots.pipe';

describe('SortObjectArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBotsPipe();
    expect(pipe).toBeTruthy();
  });
});
