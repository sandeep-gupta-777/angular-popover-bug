import { FilterObjectArrayPipe } from './highlight-session-texts.pipe';

describe('FilterObjectArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterObjectArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
