import { LinksFromTextPipe } from './links-from-text.pipe';

describe('LinksFromTextPipe', () => {
  it('create an instance', () => {
    const pipe = new LinksFromTextPipe();
    expect(pipe).toBeTruthy();
  });
});
