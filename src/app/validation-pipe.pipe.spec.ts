import { ValidationPipePipe } from './validation-pipe.pipe';

describe('ValidationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
