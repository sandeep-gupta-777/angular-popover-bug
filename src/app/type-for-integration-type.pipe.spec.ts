import { TypeForIntegrationTypePipe } from './type-for-integration-type.pipe';

describe('TypeForIntegrationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeForIntegrationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
