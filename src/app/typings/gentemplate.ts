export enum ETemplateResponseType {
  text = 'text',
  carousel = 'carousel',
  image = 'image',
  audio = 'audio',
  file = 'attach_file',
  video = 'video',
  code = 'code',
  quick_reply = 'quick_reply'
}
