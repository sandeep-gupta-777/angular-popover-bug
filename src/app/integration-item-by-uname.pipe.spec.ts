import { IntegrationItemByUnamePipe } from './integration-item-by-uname.pipe';

describe('IntegrationItemByUnamePipe', () => {
  it('create an instance', () => {
    const pipe = new IntegrationItemByUnamePipe();
    expect(pipe).toBeTruthy();
  });
});
