import { SortPipelinePipe } from './sort-pipeline.pipe';

describe('SortPipelinePipe', () => {
  it('create an instance', () => {
    const pipe = new SortPipelinePipe();
    expect(pipe).toBeTruthy();
  });
});
