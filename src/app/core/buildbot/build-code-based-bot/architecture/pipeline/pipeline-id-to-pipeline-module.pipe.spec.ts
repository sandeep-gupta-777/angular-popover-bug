import { PipelineIdToPipelineModulePipe } from './pipeline-id-to-pipeline-module.pipe';

describe('PipelineIdToPipelineModulePipe', () => {
  it('create an instance', () => {
    const pipe = new PipelineIdToPipelineModulePipe();
    expect(pipe).toBeTruthy();
  });
});
