import { StringIncludesPipe } from './string-includes.pipe';

describe('StringIncludesPipe', () => {
  it('create an instance', () => {
    const pipe = new StringIncludesPipe();
    expect(pipe).toBeTruthy();
  });
});
