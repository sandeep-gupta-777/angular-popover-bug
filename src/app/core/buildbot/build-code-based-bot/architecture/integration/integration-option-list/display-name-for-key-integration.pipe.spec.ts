import { DisplayNameForKeyIntegrationPipe } from './display-name-for-key-integration.pipe';

describe('DisplayNameForKeyIntegrationPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayNameForKeyIntegrationPipe();
    expect(pipe).toBeTruthy();
  });
});
