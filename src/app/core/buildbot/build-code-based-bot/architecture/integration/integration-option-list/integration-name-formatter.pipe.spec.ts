import { IntegrationNameFormatterPipe } from './integration-name-formatter.pipe';

describe('IntegrationNameFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new IntegrationNameFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
