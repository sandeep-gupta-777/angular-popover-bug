import { LogosByIntegrationNamePipe } from './logos-by-integration-name.pipe';

describe('LogosByIntegrationNamePipe', () => {
  it('create an instance', () => {
    const pipe = new LogosByIntegrationNamePipe();
    expect(pipe).toBeTruthy();
  });
});
