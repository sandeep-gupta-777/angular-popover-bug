import { MediaDetecionPipe } from './media-detecion.pipe';

describe('MediaDetecionPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaDetecionPipe();
    expect(pipe).toBeTruthy();
  });
});
