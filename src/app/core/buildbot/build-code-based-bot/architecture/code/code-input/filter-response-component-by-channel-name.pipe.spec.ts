import { FilterResponseComponentByChannelNamePipe } from './filter-response-component-by-channel-name.pipe';

describe('FilterResponseComponentByChannelNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterResponseComponentByChannelNamePipe();
    expect(pipe).toBeTruthy();
  });
});
