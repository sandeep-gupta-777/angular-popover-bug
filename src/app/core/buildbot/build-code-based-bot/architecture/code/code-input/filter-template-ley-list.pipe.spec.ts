import { FilterTemplateLeyListPipe } from './filter-template-ley-list.pipe';

describe('FilterTemplateLeyListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTemplateLeyListPipe();
    expect(pipe).toBeTruthy();
  });
});
