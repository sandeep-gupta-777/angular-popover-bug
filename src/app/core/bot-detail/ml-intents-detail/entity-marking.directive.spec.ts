import { EntityMarkingDirective } from './entity-marking.directive';

describe('EntityMarkingDirective', () => {
  it('should create an instance', () => {
    const directive = new EntityMarkingDirective();
    expect(directive).toBeTruthy();
  });
});
