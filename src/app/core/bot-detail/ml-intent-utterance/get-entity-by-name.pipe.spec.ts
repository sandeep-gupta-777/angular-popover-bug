import { GetEntityByNamePipe } from './get-entity-by-name.pipe';

describe('GetEntityByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetEntityByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
