import { ProfilePermissionIdToNamePipe } from './profile-permission-id-to-name.pipe';

describe('ProfilePermissionIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfilePermissionIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
