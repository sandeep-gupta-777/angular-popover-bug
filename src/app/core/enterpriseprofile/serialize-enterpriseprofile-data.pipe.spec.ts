import { SerializeEnterpriseprofileDataPipe } from './serialize-enterpriseprofile-data.pipe';

describe('SerializeEnterpriseprofileDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SerializeEnterpriseprofileDataPipe();
    expect(pipe).toBeTruthy();
  });
});
