import { IntegrationLogosPipe } from './integration-logos.pipe';

describe('IntegrationLogosPipe', () => {
  it('create an instance', () => {
    const pipe = new IntegrationLogosPipe();
    expect(pipe).toBeTruthy();
  });
});
