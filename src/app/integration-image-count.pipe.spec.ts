import { IntegrationImageCountPipe } from './integration-image-count.pipe';

describe('IntegrationImageCountPipe', () => {
  it('create an instance', () => {
    const pipe = new IntegrationImageCountPipe();
    expect(pipe).toBeTruthy();
  });
});
