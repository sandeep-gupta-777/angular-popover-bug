import { NgIfLoadingDirective } from './ng-if-loading.directive';

describe('NgIfLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIfLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
