import { SortObjectArrPipe } from './sort-object-arr.pipe';

describe('SortObjectArrPipe', () => {
  it('create an instance', () => {
    const pipe = new SortObjectArrPipe();
    expect(pipe).toBeTruthy();
  });
});
