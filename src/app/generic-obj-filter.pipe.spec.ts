import { GenericObjFilterPipe } from './generic-obj-filter.pipe';

describe('GenericObjFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GenericObjFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
