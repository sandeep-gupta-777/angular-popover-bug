import { PipelineFilterPipe } from './pipeline-filter.pipe';

describe('PipelineFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PipelineFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
