import { IntegrationItemForTypePipe } from './integration-item-for-type.pipe';

describe('IntegrationItemForTypePipe', () => {
  it('create an instance', () => {
    const pipe = new IntegrationItemForTypePipe();
    expect(pipe).toBeTruthy();
  });
});
